dependencies:
  pre:
    - npm install istanbul -g
    - npm install
    - grunt lint

general:
  artifacts:
    - "tests/coverage"

deployment:
  live:
    branch: master
    heroku:
      appname: nicejs
