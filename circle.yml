machine:
  node:
    version: 0.21.1

dependencies:
  pre:
    - sudo apt-get update; sudo apt-get install mongodb-org
    - npm install istanbul grunt-cli -g
    - npm install
    - grunt lint

general:
  artifacts:
    - "tests/coverage"

deployment:
  live:
    branch: master
    heroku:
      appname: nicejs
